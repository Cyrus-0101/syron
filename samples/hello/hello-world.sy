{
  const name = getName()
  
  write("Hello " + name + " :D")
  write("Enter a number to find the sum of all numbers from 1 to that number:")
  
  const num = input()

  write("The sum of all numbers from 1 to " + string(num) + " is " + string(sum(int(num))))
  write("The recursion sumation of " + string(num) + " is " + string(recursion(int(num))))
}